# Affirmer

##### Example

```
const describe = require("affirmer").describe;
const it = require("affirmer").it;

describe("SomeModule", () => {
  it("a piece of behavior", () => {
    console.assert(true)
  })
})
```